<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/notesList">

    <fragment
        android:id="@+id/notesList"
        android:name="habibellah.ayata.firenote.NotesList"
        android:label="fragment_notes_list"
        tools:layout="@layout/fragment_notes_list" >
        <action
            android:id="@+id/action_notesList_to_noteDescription"
            app:destination="@id/noteDescription" />
    </fragment>
    <fragment
        android:id="@+id/noteDescription"
        android:name="habibellah.ayata.firenote.NoteDescription"
        android:label="fragment_note_description"
        tools:layout="@layout/fragment_note_description" />
</navigation>